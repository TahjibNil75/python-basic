<!-- Imagine a backend where we need to send notifications via multiple channels (Email, SMS, Push).
Instead of hardcoding if-else everywhere, we use a Factory Pattern to cleanly create notification objects. -->


